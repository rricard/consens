(ns consens.cli.client
  "Interact with the cluster with the CLI")

(defn rd
  "Read a value retrieved from the information
   carried by the arguments"
  [& args]
  (println "TODO!"))

(defn write
  "Write a value retrieved and replaced from the
   information carried by the arguments"
  [& args]
  (println "TODO!"))
